This starter is a one-off transform sandbox, not an application scaffold.

- The input fixture is at `input/source.jpg`.
- Use `TRANSLOADIT_KEY` and `TRANSLOADIT_SECRET` from `process.env` when credentials are available.
- The acceptance test already exists. Do not weaken it or replace it with a different contract.
- Produce the expected output artifact under `out/`.
- Use the provided skill as the primary playbook.
- Do not replace the skill workflow with unrelated local image-processing tooling.
